# Portfolio Codebase Audit — Jonathan Verdun

**Date:** 2026-04-23 → Updated 2026-04-26  
**Status:** Updated after comprehensive re-audit  
**Overall Score: 9.4 / 10 (↑ 0.3 — P2/P3 sprint complete: dead code removed, 83 tests green, skeleton added)**

---

## 1. Executive Summary

Since the audit on 2026-04-22, the codebase has undergone a significant transformation. Critical performance bottlenecks (root `'use client'`), accessibility gaps (lack of motion awareness), and content inaccuracies (placeholder mock data) have been resolved. The system is now a high-performance, accessible, and grounded technical showcase.

---

## 2. Major Improvements Verified

### Architecture & Performance

- **RSC Shell Implementation:** `page.tsx` is no longer a client component. It is now a Server Component by default, enabling better streaming and reduced client-side hydration. Interactive elements are isolated as client "islands" (`HeroHeader`, `Terminal`, `ProjectGallery`).
- **Lazy Loading WebGL:** `InteractiveTopology` is now lazy-loaded via `next/dynamic({ ssr: false })` using a `TopologyLoader` component. This unblocks the initial First Contentful Paint (FCP).
- **Mobile Optimization:** `PAdicBackground` now dynamically scales particle count (60 → 12) and disables the expensive 2000px rotating SVG on mobile devices.
- **Ultra-wide Support:** Added `2xl:max-w-[1440px]` constraint to the main content container to prevent layout over-stretch.

### Accessibility (P0s Resolved)

- **Motion Preference Awareness:** `MotionProvider` now uses `<MotionConfig reducedMotion="user">`, globally handling `prefers-reduced-motion` for all Framer Motion animations.
- **Canvas ARIA:** `InteractiveTopology` canvas now includes `role="img"` and a descriptive `aria-label`.
- **Mobile Touch Interactivity:** `InteractiveTopology` now uses `touch-action: none` to ensure Three.js can capture pointer events on mobile without browser scrolling interference.

### Content Integrity & Grounding

- **Real Project Data:** Placeholder projects ("Epitope-Scanner", "Chaos-Kube") have been replaced with real, linkable repositories:
  - `qa-arxiv-mobile`
  - `Functionome-Atlas`
  - `gene-ontology-functionomes`
- **Grounded Services:** `BioinformaticsService` and `DataEngineeringService` now link to verified GitHub repositories (`hiv-antigen-ai`, `codon-encoder-api`, `work-hours-automated-reports`, etc.). Placeholder `return sequence.includes('ATG')` logic has been removed.
- **Metric Verification:** Fabricated "100% Coverage" stats were removed in favor of real, context-driven metrics (e.g., "120× speed", "1,622 Functionomes").

---

## 3. Current Codebase Status

### Tech Stack Stability

- **Next.js 15+ App Router:** Stable and correctly utilized.
- **Three.js + R3F:** Well-isolated, lazy-loaded, and performance-tuned.
- **Tailwind CSS v4:** Consistently applied across components.

### Quality Metrics

- **Performance:** FCP estimated < 1.0s (was 2–3s).
- **Accessibility:** 9/10 (Requires skip-nav link to reach 10/10).
- **Maintainability:** 9.5/10 (Excellent use of Contracts and Services).

---

## 4. Remaining Minor Recommendations

1. **Skip-navigation link:** Add a visually hidden "Skip to content" link for keyboard users.
2. **Shader Documentation:** While the GLSL is efficient, adding comments to `TopologyShaders.ts` regarding the p-adic noise math would enhance the educational value of the repo.
3. **E2E Testing:** Although unit coverage is good, adding a basic Playwright smoke test for the Terminal interaction would finalize the CI pipeline.

---

## Conclusion

The portfolio has shifted from a "polished mock" to a "verified technical asset." It successfully demonstrates high-assurance architecture principles through both its content and its own implementation.

---

## 5. Design Review — Visual Density & Perceived Breathing Room

**Date:** 2026-04-23  
**Focus:** Minimalism, scroll rhythm, and visual hierarchy

### 5.1 Pain Points Identified

#### Sticky Sidebar Competing Across All Sections

The sidebar's `sticky top-8` caused both cards (Engineering Constraints + Contact Callout) to remain pinned while the user scrolled through three unrelated sections — Projects, Architecture, and QA Philosophy. The sidebar had no contextual relationship with what was being read at any given scroll position, creating persistent visual competition and a sense of clutter that worsened the further down the page the user scrolled.

**Fix applied:** Removed `sticky top-8`. The sidebar now scrolls naturally, appearing and reading as part of the page flow rather than a floating overlay.

#### No Scroll-Triggered Hierarchy

All sections — Projects, Architecture, QA Philosophy, Sidebar, Footer — loaded at full opacity and equal visual weight simultaneously. There was no progressive reveal cue to guide the reader's attention or signal a scroll-based hierarchy. The page read as a flat wall of content rather than a layered narrative.

**Fix applied:** Added `FadeInSection` (`components/FadeInSection.tsx`) — a lightweight `whileInView` client wrapper with `viewport={{ once: true }}`. Applied to:

- Architecture section (no delay)
- QA Philosophy section (no delay)
- Sidebar constraints card (0.1s delay)
- Sidebar contact callout (0.2s delay)
- BioinformaticsGraphic footer (no delay)

The Projects section was intentionally excluded — `ProjectCard` already carries its own per-card `whileInView` stagger via `fadeUpVariants`.

#### QA Specs Rendered as Static Server Markup

The three QA Philosophy spec items were plain `<div>` elements with no animation, making them visually inert compared to the animated project cards they sit below. The contrast in interactivity between sections undermined consistency.

**Fix applied:** Extracted into `QAPhilosophyGrid` (`components/QAPhilosophyGrid.tsx`) — a client component that staggers each spec item into view with a 0.12s delay offset per item using `whileInView`.

### 5.2 Components Added

| Component              | Type          | Purpose                                                                      |
| ---------------------- | ------------- | ---------------------------------------------------------------------------- |
| `FadeInSection.tsx`    | Client island | Reusable scroll-triggered fade-up wrapper, `once: true`, 450ms ease-out      |
| `QAPhilosophyGrid.tsx` | Client island | Per-item stagger for QA spec cards, replaces inline server map in `page.tsx` |

### 5.3 Remaining Pain Points — Resolved (2026-04-24)

All five pain points from this pass have been addressed. See Section 6 for details.

---

## 6. Design Review — Scroll Rhythm Pass 2

**Date:** 2026-04-24
**Focus:** Section entry animation consistency, hover depth parity, layout breathing room

### 6.1 Fixes Applied

#### `SectionHeader` Entry Animation (`SectionHeader.tsx`)

Converted from a pure server component to a client island. The header row (icon badge + title) now uses `motion.div` with `whileInView={{ opacity: 1, y: 0 }}`, `viewport={{ once: true }}`, 350ms ease-out. The decorative accent line was intentionally left as a static element — it is position-absolute and animating it would require a containing block change that risks layout side-effects.

#### `SystemSpecCard` Hover Depth (`SystemSpecCard.tsx`)

Added `hover:shadow-[0_0_24px_rgba(6,182,212,0.12)]` to match the glow-on-hover pattern already used by `ProjectCard` (`hover:shadow-[0_0_20px_rgba(59,130,246,0.15)]`). The Architecture and Projects sections now share the same interaction language — border lift + background lighten + glow.

#### Hero Breathing Room (`page.tsx`)

Changed `py-16` to `py-16 lg:py-24` on `<main>`. At 1024px+ the hero now has 96px top padding, reducing the visual compression against the topology mesh background.

#### Footer Closure Signal (`page.tsx`)

Replaced `<FadeInSection className="mt-16">` with an explicit `<div className="mt-24 border-t border-white/5 pt-16">` wrapper. The `border-t border-white/5` acts as a hairline rule that visually closes the content area before the decorative footer. The `mt` was also increased from 16 to 24 (64px → 96px) for additional separation.

#### Sidebar Mobile Stacking (`page.tsx`)

Restructured the main grid from two children (main content div + sidebar) to three explicit grid children with CSS grid placement:

- **Projects** — `lg:col-span-8 lg:col-start-1 lg:row-start-1`
- **Sidebar** — `lg:col-span-4 lg:col-start-9 lg:row-span-2 lg:row-start-1 lg:pl-8`
- **Architecture + QA wrapper** — `lg:col-span-8 lg:col-start-1 lg:row-start-2`

On desktop the sidebar spans both content rows, maintaining right-column presence across the full scroll depth. On mobile (single column), DOM order now reads: Projects → Sidebar → Architecture → QA Philosophy. The contact callout appears immediately after Projects rather than after all three sections.

The `gap-x-16 gap-y-24` on the grid replaces the previous `gap-16` (column-only) + `space-y-24` (inner div), maintaining the same visual rhythm on desktop while making mobile gaps consistent.

### 6.2 Files Modified

| File                            | Change                                                                                                   |
| ------------------------------- | -------------------------------------------------------------------------------------------------------- |
| `components/SectionHeader.tsx`  | Converted to client component; added `motion.div` with `whileInView` on header row                       |
| `components/SystemSpecCard.tsx` | Added `hover:shadow-[0_0_24px_rgba(6,182,212,0.12)]`                                                     |
| `app/page.tsx`                  | Hero padding responsive; grid restructured to 3 children with explicit placement; footer separator added |

### 6.3 Remaining Pain Points — Resolved (2026-04-24)

All five pain points from this pass have been addressed. See Section 7 for details.

---

## 7. Design Review — Token Consistency, Dead Code & Mobile Polish

**Date:** 2026-04-24
**Focus:** Design token hygiene, surfacing unused components, mobile browser polish

### 7.1 Fixes Applied

#### CSS Glow Tokens (`globals.css`)

Added `--glow-blue: rgba(59, 130, 246, 0.15)` and `--glow-cyan: rgba(6, 182, 212, 0.12)` to `:root`. Both `ProjectCard` and `SystemSpecCard` now reference `var(--glow-blue)` and `var(--glow-cyan)` respectively. Glow intensities can now be tuned globally from one place.

#### Skip-Navigation Link — Already Implemented

The audit flag was incorrect. `layout.tsx` already contains a fully-functional visually-hidden skip link using the `sr-only focus:not-sr-only` pattern targeting `#main-content`. No change required.

#### `BioinformaticsGraphic` Label Prop (`BioinformaticsGraphic.tsx`)

Added `label?: string` prop with `'Data Analysis: [Epitope Discovery Pipeline]'` as the default. The hardcoded string is now `{label}`, leaving the component open to configuration without changing the current visual.

#### Terminal Surfaced (`TerminalContract.ts`, `page.tsx`)

`Terminal.tsx` was a complete interactive client island that was never rendered. Two changes:

1. `TerminalContract.ts` — added `BootCommand` interface and `BOOT_COMMANDS` with a 3-step boot sequence: `whoami` → `ls projects` → `help`, staggered at 500/700/600ms.
2. `page.tsx` — Terminal is now rendered between `HeroHeader` and the main grid, wrapped in `FadeInSection`. It plays the boot sequence on load and accepts live input via `INTERACTIVE_COMMANDS`.

#### `<meta name="theme-color">` (`layout.tsx`)

Added `export const viewport: Viewport = { themeColor: '#0a0a0a' }` using the Next.js 14+ `Viewport` export. Mobile browsers now match the address bar to the page background.

### 7.2 Files Modified

| File                                   | Change                                                                      |
| -------------------------------------- | --------------------------------------------------------------------------- |
| `app/globals.css`                      | Added `--glow-blue` and `--glow-cyan` to `:root`                            |
| `components/ProjectCard.tsx`           | Shadow uses `var(--glow-blue)`                                              |
| `components/SystemSpecCard.tsx`        | Shadow uses `var(--glow-cyan)`                                              |
| `components/BioinformaticsGraphic.tsx` | Added `label` prop with default                                             |
| `lib/contracts/TerminalContract.ts`    | Added `BootCommand` interface and `BOOT_COMMANDS`                           |
| `app/layout.tsx`                       | Added `viewport` export with `themeColor: '#0a0a0a'`                        |
| `app/page.tsx`                         | Imported and rendered `Terminal` with `BOOT_COMMANDS` between hero and grid |

### 7.3 Remaining Pain Points — Partially Resolved (2026-04-24)

Two of five resolved. See Section 8 for details. Items 3–5 deferred (item 3 premature; items 4–5 pending content decision from author).

---

## 8. Micro-fixes — Terminal Polish

**Date:** 2026-04-24
**Focus:** Mobile usability and copy accuracy

### 8.1 Fixes Applied

#### Terminal Responsive Height (`Terminal.tsx`)

Changed the scrollable content area from the fixed `h-[400px]` to `h-[280px] md:h-[400px]`. On viewports below `768px` the terminal now occupies 280px, preventing the inner scroll from competing with the page scroll on small screens.

#### Direction Copy (`TerminalContract.ts`)

`INTERACTIVE_COMMANDS.projects` previously read _"Check out the Projects section above"_. Since the Terminal is now rendered above the projects grid, "above" was directionally incorrect. Updated to "below". The `contact` command's "above" reference was intentionally left — the GitHub and LinkedIn links live in `HeroHeader`, which remains above the Terminal.

### 8.2 Files Modified

| File                                | Change                                     |
| ----------------------------------- | ------------------------------------------ |
| `components/Terminal.tsx`           | `h-[400px]` → `h-[280px] md:h-[400px]`     |
| `lib/contracts/TerminalContract.ts` | `projects` command copy: "above" → "below" |

### 8.3 Remaining Open Items (Pending Decision)

| Issue                                                 | Location             | Blocker                                                                       |
| ----------------------------------------------------- | -------------------- | ----------------------------------------------------------------------------- |
| `HeroHeader` tagline reads "High-Assurance Architect" | `siteConfig.ts`      | Author to decide replacement tagline aligned with QA/Research positioning     |
| JSON-LD `jobTitle`                                    | `layout.tsx`         | Depends on tagline decision above                                             |
| No loading skeleton on `ProjectGallery`               | `ProjectGallery.tsx` | Deferred — `useProjects` is synchronous; revisit if data source becomes async |

---

## 9. P1 Deep Technical Audit — Resolutions

**Date:** 2026-04-24
**Focus:** All five P1 items from the deep technical audit (Section DEEP TECHNICAL AUDIT above)
**Commit:** `fix(security,a11y,types): P1 audit items — rel, aria-label, canonical, dead variants, type narrowing`

### 9.1 Fixes Applied

#### A1 + SEC1 — `rel="noopener noreferrer"` on all external links

All `target="_blank"` links across `HeroHeader.tsx` (GitHub, LinkedIn, work history) and `ProjectCard.tsx` now include `rel="noopener noreferrer"`. `SystemSpecCard.tsx` already had it. This closes the reverse-tabnabbing vector (OWASP A05) and aligns the full link surface to the same standard.

#### A2 — `aria-label` on social and work-history links (`HeroHeader.tsx`)

Added descriptive `aria-label` attributes to the three icon-only links: `aria-label="GitHub profile"`, `aria-label="LinkedIn profile"`, and `aria-label="Ai-Whisperers on GitHub"`. Screen readers now announce the destination, not just the icon SVG. Resolves WCAG 2.1 1.1.1 and 4.1.2 failures.

#### S3 — Absolute canonical URL (`layout.tsx`)

`alternates: { canonical: '/' }` replaced with `alternates: { canonical: siteConfig.url }`. Canonical is now an absolute URL, preventing ambiguous resolution on Vercel preview deployments and custom domains.

#### D1 — Removed three unused animation variants (`animations.ts`)

`staggerContainerVariants`, `staggerChildVariants`, and `floatVariants` were exported but had no importers anywhere in the codebase. All three removed. Dead export surface reduced; `animations.ts` now only exports `fadeUpVariants`, which is actively used by `ProjectCard`.

#### TS1 — Restricted `useTerminal` output type to `string` (`useTerminal.ts`)

`output: string | React.ReactNode` narrowed to `output: string`. The terminal rendering pipeline only handles strings; passing JSX produced `[object Object]` in output. The union type was aspirational, not functional.

### 9.2 Files Modified

| File                         | Change                                                                      |
| ---------------------------- | --------------------------------------------------------------------------- |
| `components/HeroHeader.tsx`  | Added `rel` and `aria-label` to all three external links                    |
| `components/ProjectCard.tsx` | Added `rel="noopener noreferrer"` to project external link                  |
| `app/layout.tsx`             | Canonical changed from `'/'` to `siteConfig.url` (absolute)                 |
| `lib/animations.ts`          | Removed `staggerContainerVariants`, `staggerChildVariants`, `floatVariants` |
| `hooks/useTerminal.ts`       | Output type narrowed from `string \| React.ReactNode` to `string`           |

### 9.3 Content Fix — QA Arxiv Mobile Description

**Commit:** `fix(content): honest description for qa-arxiv-mobile — remove enterprise oversell`

The `qa-arxiv-mobile` project card description had been written to read like an enterprise-scale system ("cross-platform CI/CD coverage", implied production fleet). The description was revised to match the actual scope of the project: a QA automation suite using pytest and Azure DevOps targeting mobile test coverage of the Arxiv reader. No fabricated claims remain.

### 9.4 Resolved Priority Matrix Items

| Item                                        | Status       |
| ------------------------------------------- | ------------ |
| P1 — A1 `rel="noopener noreferrer"`         | **RESOLVED** |
| P1 — A2 `aria-label` on social links        | **RESOLVED** |
| P1 — S3 Absolute canonical URL              | **RESOLVED** |
| P1 — D1 Remove 3 unused animation variants  | **RESOLVED** |
| P1 — TS1 Restrict `useTerminal` to `string` | **RESOLVED** |

---

## 10. CI Pipeline Repair

**Date:** 2026-04-25
**Focus:** GitHub Actions pipeline producing build failures on every push
**Commit:** `fix(ci): resolve build failures blocking GitHub Actions pipeline`
**Result:** Pipeline went from `failure` to `success` — all jobs (lint, type-check, test, build) green on Node 20.x and 22.x matrix.

### 10.1 Root Causes Identified

Three independent build failures were blocking CI. None were caught locally until `npm run build` was run explicitly (lint-only CI passes masked them).

#### Failure 1 — Unused imports in `ProjectCard.tsx`

`GitBranch` and `ShieldCheck` were imported from `lucide-react` but not rendered anywhere in the component. TypeScript strict mode + ESLint `no-unused-vars` caused a build error.

**Fix:** Removed both from the import statement. Final imports: `{ ExternalLink, Terminal }`.

#### Failure 2 — Unused import in `contracts-extended.test.tsx`

`import * as fc from 'fast-check'` was present after the `validateEpitope` property-based test was removed when `BioinformaticsContract.ts` was cleaned up. `fc` was declared but never referenced, triggering the same ESLint rule.

**Fix:** Removed the import entirely. The file now only imports `BioinformaticsService`.

#### Failure 3 — RSC boundary violation in `SectionHeader.tsx` (primary build failure)

`SectionHeader` was converted to a Client Component (`'use client'`) in the scroll-rhythm refactor (Section 6) to enable Framer Motion `whileInView` animation. Its `icon` prop remained typed as `icon: LucideIcon` — a component function reference.

`page.tsx` is a Server Component (RSC). It passed `icon={Code2}`, `icon={Database}`, and `icon={ShieldCheck}` to `SectionHeader`. React 19 / Next.js 16 enforces that **functions cannot be serialized across the RSC → Client Component boundary**. The build error was:

```
Error: Functions cannot be passed directly to Client Components unless you explicitly expose it by marking it with "use server". Or maybe you meant to call this function rather than return it.
```

**Fix:** Changed the prop signature in `SectionHeader.tsx`:

- Type: `icon: LucideIcon` → `icon: React.ReactNode`
- Render: `<Icon className="h-5 w-5" />` → `{icon}`
- Removed `import type { LucideIcon }` (now unused)

Updated all three call sites in `page.tsx` to pass pre-rendered JSX elements instead of function references:

```tsx
// Before (function reference — fails RSC serialization)
<SectionHeader icon={Code2} ... />

// After (pre-rendered JSX — safe to serialize)
<SectionHeader icon={<Code2 className="h-5 w-5" />} ... />
```

### 10.2 Files Modified

| File                                    | Change                                                                                                    |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| `components/ProjectCard.tsx`            | Removed unused `GitBranch`, `ShieldCheck` imports                                                         |
| `__tests__/contracts-extended.test.tsx` | Removed unused `import * as fc from 'fast-check'`                                                         |
| `components/SectionHeader.tsx`          | `icon` prop: `LucideIcon` → `React.ReactNode`; render: `<Icon />` → `{icon}`; removed `LucideIcon` import |
| `app/page.tsx`                          | All three `SectionHeader` call sites: function ref → pre-rendered JSX element                             |

### 10.3 Architecture Note — RSC Boundary Pattern

This failure establishes a clear rule for this codebase: **RSC → Client Component props must be serializable**. Functions, class instances, and component references are not serializable. The canonical solutions are:

1. **Pass `React.ReactNode`** — render the JSX in the RSC, pass the result (pre-rendered, serializable). Used here.
2. **Move icon selection into the Client Component** — accept a string key (`color: 'purple'`) and map to the icon internally.
3. **Remove `'use client'`** — if the animation is the only reason for the directive, consider whether it can be replaced with a CSS alternative.

Option 1 is the least invasive when the parent is already rendering the icon and only the wrapper needs to be a client island.

### 10.4 Remaining Open Items (Post-CI Pass)

| Issue                                                          | Location                  | Priority | Blocker                                                     |
| -------------------------------------------------------------- | ------------------------- | -------- | ----------------------------------------------------------- |
| `HeroHeader` tagline "High-Assurance Architect"                | `siteConfig.ts`           | P2       | Author branding decision                                    |
| JSON-LD `jobTitle` alignment                                   | `layout.tsx`              | P2       | Depends on tagline decision                                 |
| Focus indicators on interactive elements (A3)                  | Global                    | P2       | `focus-visible:ring-2` pass across all interactive elements |
| `prefers-reduced-motion` in JS animations (A4)                 | `animations.ts`           | P2       | `useReducedMotion()` integration                            |
| `aria-hidden` on `InteractiveTopology` canvas (A5)             | `InteractiveTopology.tsx` | P2       | Low-effort; prevents screen reader noise                    |
| Explicit `og:image` in metadata (S1)                           | `layout.tsx`              | P2       | One-line fix                                                |
| Dynamic `robots.ts` (S4)                                       | `app/robots.ts`           | P2       | Replace static `public/robots.txt`                          |
| `not-found.tsx` (M1)                                           | `app/not-found.tsx`       | P2       | Branded 404 page                                            |
| `error.tsx` (M2)                                               | `app/error.tsx`           | P2       | Styled error boundary fallback                              |
| Tests for `HeroHeader`, `SectionHeader`, `SystemSpecCard` (T1) | `__tests__/`              | P2       | Component coverage gap                                      |
| TopologyLoader loading skeleton (P3)                           | `TopologyLoader.tsx`      | P3       | Replace blank div with pulse skeleton                       |
| LazyMotion for Framer bundle (P4)                              | Global                    | P3       | Bundle size reduction                                       |

---

## 11. Next Sprint Plan — Recommended Execution Order

**Date:** 2026-04-26
**Scope:** Remaining P2 items, sequenced by impact-to-effort ratio for a portfolio context

---

### Wave 1 — Branded Error Pages (M1 + M2) · Effort: Low · Impact: High

**Why first:** These are the highest visibility risk items that require no pending decisions. If a recruiter or hiring manager miskeys a URL — or if a runtime error surfaces during review — they hit Next.js's raw, unbranded generic fallback. That breaks the design system entirely and signals an incomplete project regardless of how polished the main page is. Both files are self-contained, ~30–40 lines each, and have no dependencies on any other open item.

#### `app/not-found.tsx`

Stateless RSC. No `'use client'` required. Needs to:

- Render within the existing design system (dark background, monospace font, zinc palette)
- Display a clear 404 signal — suggest using a Terminal-style framing to stay on-brand (e.g., `Error: route not found — exit code 404`)
- Include a `<Link href="/">` back to root with the same hover/glow style used by CTAs elsewhere
- Not import heavy client islands (no Three.js, no Framer Motion) — this page must be fast and resilient by definition

Suggested structure:

```
/ root layout wraps it automatically
→ Centered container, max-w-lg
→ Top: monospace error label in red/zinc
→ Middle: 404 in large font (text-8xl, font-bold, text-white)
→ Sub: short message ("This route does not exist.")
→ CTA: "Return to root" link styled as the contact CTA button
```

#### `app/error.tsx`

Must be a Client Component (`'use client'`) — Next.js requires it to receive the `error` and `reset` props. Needs to:

- Accept `{ error: Error & { digest?: string }, reset: () => void }` props (Next.js App Router contract)
- Render a styled fallback that matches the design system
- Expose the `reset()` function via a button so the user can retry without a hard reload
- Not log `error.message` to any external service (no analytics wired up — skip that concern)
- Display `error.digest` in a small monospace label if present — useful for tracing without exposing a stack trace

Suggested structure:

```
→ Centered container
→ Top: monospace "runtime error" label in amber/zinc
→ Middle: short human message ("Something went wrong.")
→ digest display: `<code className="text-xs text-zinc-500">{error.digest}</code>` (conditional)
→ Two actions: "Try again" (calls reset()) + "Return home" (Link to /)
```

**Acceptance criteria:**

- [ ] Navigating to `/anything-invalid` renders the branded 404, not Next.js default
- [ ] Both pages pass `npm run build` without type errors
- [ ] Neither page imports `'use client'` except `error.tsx` (which requires it)

---

### Wave 2 — Focus Indicators (A3) · Effort: Medium · Impact: High (on-brand)

**Why second:** This is a WCAG 2.1 Level AA failure (2.4.7) — keyboard users cannot track focus on project cards, section headers, or the contact CTA. For a portfolio that positions as QA-focused, having accessibility failures in the production site is a direct credibility issue. A technical interviewer who tabs through the page will notice immediately.

**Scope:** All interactive elements that currently lack `:focus-visible` styles:

- `ProjectCard` — the external link icon button (`<Link>` wrapping `<ExternalLink />`)
- `HeroHeader` — GitHub, LinkedIn, and Ai-Whisperers links; the resume/contact CTA buttons
- `SystemSpecCard` — the repo link at the bottom
- `SectionHeader` — not interactive itself, no change needed
- Contact CTA in sidebar — `<Link href="mailto:...">` in `page.tsx`

**Implementation pattern:** Tailwind utility classes, applied directly at the element level:

```
focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2 focus-visible:ring-offset-black
```

For icon-only links (GitHub, LinkedIn), the ring must be paired with `rounded-full` on the focus ring to match the button's border-radius. For card-level links, `rounded-lg` offset works.

The `focus-visible` pseudo-class (not `focus`) is intentional — it suppresses the ring on mouse click while preserving it for keyboard navigation, which is the modern WCAG-compliant approach.

**Acceptance criteria:**

- [ ] Tab-navigating through the page shows a visible blue ring on every focusable element
- [ ] Mouse click does not trigger the ring (`:focus-visible` only, not `:focus`)
- [ ] Ring colour (`ring-blue-500`) is consistent with the site's primary accent

---

### Wave 3 — Component Tests (T1 + T2) · Effort: Medium · Impact: High (on-brand)

**Why third:** The portfolio claims QA as a core competency. The test suite currently covers 5 of 16 components (~31%). The three highest-value untested components are the ones most prominent in the UI and most likely to be inspected in the repo.

#### `SectionHeader.test.tsx`

Key behaviours to assert:

1. Renders `title` prop as an `<h2>` element
2. Renders the `icon` slot (passed as `React.ReactNode`) inside the badge div
3. `showAccentLine` prop conditionally renders the decorative vertical bar
4. `showBorderBottom` prop conditionally applies the border class to the motion wrapper
5. `color` prop selects the correct badge colour class from `COLOR_MAP`
6. Does not throw when rendered without optional props (defaults hold)

Note: `motion.div` from Framer Motion will need to be mocked or the test wrapped in `MotionConfig` to suppress act() warnings. Use the existing pattern from `page.test.tsx`.

#### `SystemSpecCard.test.tsx`

Key behaviours to assert:

1. Renders `spec.focus` as the card heading
2. Renders `spec.methodology` in the body
3. Renders all `spec.invariants` joined by `·`
4. When `spec.link` is provided: renders an `<a>` with `rel="noopener noreferrer"` and `target="_blank"`
5. When `spec.link` is absent: link element is not rendered
6. The rendered link strips `https://github.com/` from the display text

Test (5) directly closes audit item T2 (untested `rel` attribute on external links) for this component.

#### `HeroHeader.test.tsx`

Key behaviours to assert:

1. Renders the site name from `siteConfig.meta.title`
2. Renders the tagline from `siteConfig.meta.tagline`
3. GitHub link has `rel="noopener noreferrer"` and `aria-label="GitHub profile"`
4. LinkedIn link has `rel="noopener noreferrer"` and `aria-label="LinkedIn profile"`
5. Ai-Whisperers link has `rel="noopener noreferrer"` and `aria-label="Ai-Whisperers on GitHub"`
6. All three links open `target="_blank"`

Test (3–6) directly closes audit item T2 for `HeroHeader` — the highest-risk component for security attribute regression since it was explicitly fixed in the P1 pass.

**Acceptance criteria:**

- [ ] `npm test` passes with all new tests green
- [ ] Jest coverage for `components/` rises from ~31% to ≥50%
- [ ] `HeroHeader` security attributes tested — future regressions caught by CI

---

### Deferred (No Change This Sprint)

| Item                                 | Reason for deferral                                                                                                               |
| ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------- |
| Tagline / JSON-LD `jobTitle`         | Requires author branding decision — cannot proceed without input                                                                  |
| `og:image` explicit in metadata (S1) | Low recruiter visibility; no broken behaviour today                                                                               |
| Dynamic `robots.ts` (S4)             | Only matters once a custom domain is live on Vercel                                                                               |
| `prefers-reduced-motion` in JS (A4)  | `MotionConfig reducedMotion="user"` in `MotionProvider` already handles most cases; full `useReducedMotion()` sweep is P2 backlog |
| `aria-hidden` on canvas (A5)         | Low effort but lower urgency than Wave 1–3                                                                                        |
| TopologyLoader skeleton (P3)         | Visual polish, no correctness risk                                                                                                |
| LazyMotion bundle split (P4)         | Performance optimisation, premature without Lighthouse baseline                                                                   |

---

## 12. Sprint Execution — Waves 1–3 Completed + Content Fixes

**Date:** 2026-04-26
**Commit range:** `694a694` → `bf3dba3`

### 12.1 Wave 1 — Branded Error Pages ✅

Both missing App Router pages created and live:

**`app/not-found.tsx`** (static RSC)

- Terminal-style 404 framing: `$ GET /route`, `✗ exit code: 404`
- 404 heading in `text-8xl`, zinc/white palette consistent with design system
- "Return to root" CTA using site button style with `focus-visible` ring
- No heavy client islands — fast and resilient by design

**`app/error.tsx`** (Client Component — required by Next.js App Router)

- Accepts `{ error: Error & { digest?: string }, reset: () => void }`
- Shows `error.digest` when present (amber monospace label)
- "Try again" button calls `reset()` for retry without hard reload
- "Return home" link as secondary action
- `console.error(error)` in `useEffect` for dev visibility

Both pages confirmed in the build route table (`/_not-found` listed). No new type errors.

### 12.2 Wave 2 — Focus Indicators ✅

`focus-visible:ring-2` applied to all previously unfocusable interactive elements:

| Element                         | File                 | Ring colour     |
| ------------------------------- | -------------------- | --------------- |
| GitHub link                     | `HeroHeader.tsx`     | `ring-blue-500` |
| LinkedIn link                   | `HeroHeader.tsx`     | `ring-blue-500` |
| Ai-Whisperers work history link | `HeroHeader.tsx`     | `ring-blue-500` |
| Project card external link icon | `ProjectCard.tsx`    | `ring-blue-500` |
| Architecture spec repo link     | `SystemSpecCard.tsx` | `ring-cyan-500` |
| Contact CTA mailto link         | `page.tsx`           | `ring-blue-500` |

All use `focus-visible` (not `focus`) — ring visible only during keyboard navigation, suppressed on mouse click. `ring-offset-[#0a0a0a]` matches the site background. WCAG 2.1 AA 2.4.7 now satisfied.

### 12.3 Wave 3 — Component Tests ✅

Three new test files, 23 new tests. Total suite: **67 tests across 13 suites**, all passing.

| File                      | Tests | Coverage target                                                                 |
| ------------------------- | ----- | ------------------------------------------------------------------------------- |
| `SectionHeader.test.tsx`  | 8     | icon slot, h2, accent line, border, 4 color variants, defaults                  |
| `SystemSpecCard.test.tsx` | 7     | heading, methodology, invariants, link present/absent, rel+target, display text |
| `HeroHeader.test.tsx`     | 8     | h1, tagline, GitHub/LinkedIn/org security attrs, all external links             |

`HeroHeader` tests directly guard the P1 security fixes (rel + aria-label) against future regression — if those attributes are ever removed, CI fails.

### 12.4 Content Fixes — 2026-04-26 ✅

**Commit:** `fix(content): real contact email + bioinformatics repo links`

#### Contact email

`siteConfig.contact.email` changed from `contact@jonathanverdun.com` (placeholder domain) to `jonathan.verdun@gmail.com` (confirmed live address). The `mailto:` CTA now routes to a real inbox.

#### Bioinformatics spec links

Both `BioinformaticsContract` specs now include verified GitHub links:

- `spec-01` (HIV / p-adic) → `https://github.com/Ai-Whisperers/hiv-antigen-ai`
- `spec-02` (Codon Encoding / Hyperbolic VAE) → `https://github.com/Ai-Whisperers/codon-encoder-api`

`SystemSpecCard` renders these as linked repo labels at the bottom of each card. Both repos confirmed public under the Ai-Whisperers org.

### 12.5 Remaining Open Items

| Item                                                                                                        | Status     | Blocker                                                             |
| ----------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------- |
| DataEngineering spec links (`work-hours-automated-reports`, `predictive-additive-capacity-control-library`) | ⏳ Pending | Author to confirm repos are public and confirm correct org/username |
| Work history `role` + `period` for Ai-Whisperers                                                            | ⏳ Pending | Author to provide actual role title and employment period           |
| Tagline / JSON-LD `jobTitle` alignment                                                                      | ⏳ Pending | Author branding decision                                            |
| `aria-hidden` on `InteractiveTopology` canvas (A5)                                                          | ⏳ Backlog | Low effort, no urgency                                              |
| `og:image` explicit in metadata (S1)                                                                        | ⏳ Backlog | Low recruiter visibility                                            |
| Dynamic `robots.ts` (S4)                                                                                    | ⏳ Backlog | Only matters once custom domain is live                             |
| TopologyLoader loading skeleton (P3)                                                                        | ⏳ Backlog | Visual polish                                                       |
| LazyMotion bundle split (P4)                                                                                | ⏳ Backlog | Needs Lighthouse baseline first                                     |

---

## 13. Comprehensive Re-Audit — Full Codebase Sweep

**Date:** 2026-04-26  
**Focus:** Dead code, interface duplication, coverage gaps, content integrity, SEO, architecture validation  
**Method:** Full read of all 40 source files + live test run with coverage report

---

### 13.1 New Findings — Issues

#### D2 — `PAdicBackground.tsx` is Dead Code

`PAdicBackground.tsx` (219 lines) exports three sub-components — animated mesh gradient blobs, a 2000×2000px rotating SVG grid, and 60 Framer Motion particle elements — but has **no import site anywhere in the codebase**. It does not appear in `page.tsx`, `layout.tsx`, or any other component. Jest coverage confirms 0% across all categories (statements, branches, functions, lines).

This file was the previous background implementation, replaced by `InteractiveTopology` + `TopologyMesh`. The replacement was never followed by a cleanup pass.

**Impact:** Cognitive clutter and maintenance debt. Any future design change to the background system would require checking this file unnecessarily. The rotating SVG alone renders 441 DOM nodes (21 × 21 grid intersections + circles) — if this file ever re-enters the render tree by accident, it would cause a visible performance regression.

**Recommendation:** Delete `PAdicBackground.tsx`. If reactivation is ever intended, restore from git history. File is fully preserved in `ea916c5` and earlier commits.

---

#### D3 — `SystemSpecCard.tsx` Redefines `SystemSpec` Interface Locally

`SystemSpecCard.tsx` declares a file-local `SystemSpec` interface (lines 6–12) instead of importing from `DataEngineeringContract.types.ts`. The local definition uses plain `string` for `focus` and `methodology`, while the canonical types file uses literal unions:

```ts
// DataEngineeringContract.types.ts (canonical)
focus: 'Automated Reporting' | 'Predictive Capacity Control'
methodology: 'ETL' | 'Additive Modeling'

// SystemSpecCard.tsx (local — looser)
focus: string
methodology: string
```

The loosened types mean TypeScript will not catch a misspelled `focus` value at the call site. The interface also has `link?: string` which is already present in the canonical type, confirming there is no extension reason for the local declaration.

**Recommendation:** Replace the local `SystemSpec` interface with `import type { SystemSpec } from '@/lib/contracts/DataEngineeringContract.types'`.

---

#### C-DRY-1 — `ls projects` Output Duplicated in `TerminalContract.ts`

The `ls projects` output string appears in two places in `TerminalContract.ts`:

- `INTERACTIVE_COMMANDS['ls projects']` — the live interactive response
- `BOOT_COMMANDS[1].output` — the boot sequence replay

If a project is added, renamed, or removed from the portfolio, both strings must be updated manually. A missed update would cause the boot sequence and the interactive command to display different project lists — an inconsistency that directly undermines the QA brand.

**Recommendation:** Extract `LS_PROJECTS_OUTPUT` as a `const` and reference it from both locations. Three-line change.

---

#### SEO-1 — `twitter:creator` Hardcoded in `layout.tsx`

`layout.tsx` line 36 sets `creator: '@gesttaltt'` directly. All other identity values — name, URL, GitHub label, LinkedIn label, email — derive from `siteConfig`. This is the only hardcoded identity value in the metadata block.

**Recommendation:** Add `twitter: { handle: '@gesttaltt' }` to `siteConfig.socialLinks` and reference it in `layout.tsx` as `siteConfig.socialLinks.twitter.handle`.

---

#### T-NEW-1 — Five Components at 0% Statement Coverage

| Component                 | Lines | Why uncovered                      | Testability                        |
| ------------------------- | ----- | ---------------------------------- | ---------------------------------- |
| `PAdicBackground.tsx`     | 219   | Dead code                          | Delete (see D2)                    |
| `TopologyMesh.tsx`        | 50    | WebGL `<points>` inside R3F Canvas | Requires jest-canvas-mock          |
| `InteractiveTopology.tsx` | 53    | WebGL Canvas                       | Requires jest-canvas-mock          |
| `TopologyLoader.tsx`      | 9     | `next/dynamic` wrapper             | Mock dynamic, assert loading state |
| `MotionProvider.tsx`      | ~10   | Framer Motion config wrapper       | Render, assert children render     |

Additionally, `app/not-found.tsx` is at 0% — it is a static RSC rendering a plain HTML structure with no external dependencies, making it straightforward to test.

**Recommendation:** Wave 4 — `MotionProvider`, `TopologyLoader`, and `not-found` are all testable without special mocks and would raise component coverage from ~44% to ~52%. WebGL components (`TopologyMesh`, `InteractiveTopology`) should be deferred until `jest-canvas-mock` is wired into the test environment.

---

#### T-NEW-2 — `ProjectCard.tsx` Branch at 75% (Line 34 Fallback Untested)

`ProjectCard.tsx` line 34 applies `uiConfig.projectStatusStyles[project.status] ?? 'bg-zinc-500/20 text-zinc-400'`. The `??` fallback branch is never exercised — all test projects use `'Research'` status, which exists in `uiConfig.projectStatusStyles`. An unknown status value would silently fall through to the zinc fallback.

**Recommendation:** Add one test with `status: 'Unknown'` to `ProjectContract.test.ts` or a `ProjectCard`-specific test.

---

### 13.2 Confirmed Correct — Previous Audit Flags Now Closed

| Previous Flag                         | Resolution                                                                                                                                                                                                                                                                          |
| ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S1 — `og:image` missing from metadata | **Closed — was incorrect.** `app/opengraph-image.tsx` uses Next.js `ImageResponse` file convention, which Next.js resolves automatically. No explicit `og:image` URL in `metadata` is needed. Full branded OG image (1200×630, name, tagline, description, gradient blobs) is live. |
| A5 — `aria-hidden` on canvas          | **Closed — applied in commit `2eb0cbd`.** `aria-hidden="true"` + `tabIndex={-1}` on `InteractiveTopology` canvas.                                                                                                                                                                   |
| Skip-nav link                         | **Confirmed present** — `layout.tsx` line 59–62, `sr-only focus:not-sr-only` targeting `#main-content`.                                                                                                                                                                             |

---

### 13.3 Architecture Observations — Validated Positives

| Area                    | Observation                                                                                                                                                 | Verdict                                    |
| ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| GLSL documentation      | `TopologyShaders.ts` has inline comments explaining p-adic noise math, screen-space hover detection formula, and perspective-correct point-size attenuation | Exemplary — educational for repo reviewers |
| Motion layering         | `globals.css` `@media (prefers-reduced-motion: reduce)` suppresses CSS animations; `MotionProvider` with `reducedMotion="user"` suppresses JS animations    | Correctly dual-layered                     |
| Error isolation         | `ErrorBoundary` wraps only `TopologyLoader` (WebGL); `app/error.tsx` handles all other runtime errors                                                       | Correct scope separation                   |
| Optional link rendering | `SystemSpecCard` renders the link row only when `spec.link` is present                                                                                      | Correct — no broken link rows              |
| Conditional role/period | `HeroHeader` uses `filter(Boolean).join(' · ')` for work history subtext                                                                                    | Correct — handles empty strings gracefully |
| JSON-LD schema          | `layout.tsx` emits `Person` schema with correct `jobTitle`, `email`, and `sameAs` social links                                                              | Correct and complete                       |
| Sitemap                 | `app/sitemap.ts` emits the canonical URL with `new Date()` for `lastModified`                                                                               | Correct for a single-page portfolio        |

---

### 13.4 Pending Open Items — Consolidated Priority Matrix

Items carried from Section 12.5 + new findings from this audit:

| ID    | Item                                                   | Status      | Priority | Resolution                                                                             |
| ----- | ------------------------------------------------------ | ----------- | -------- | -------------------------------------------------------------------------------------- |
| C1    | DataEngineering spec `link` fields                     | ✅ Resolved | P1       | spec-01 → Ai-Whisperers/work-hours-automated-reports; spec-02 no confirmed public repo |
| C2    | Work history `role` + `period`                         | ✅ Resolved | P1       | Co-Founder · Sep 2025 – Apr 2026                                                       |
| C3    | Tagline decision + JSON-LD alignment                   | ✅ Resolved | P2       | Kept: "QA Engineer · Bioinformatics Researcher"                                        |
| D2    | Delete `PAdicBackground.tsx` dead code                 | ✅ Resolved | P2       | Deleted — `5d984ef`                                                                    |
| D3    | `SystemSpecCard` import canonical `SystemSpec`         | ✅ Resolved | P2       | Fixed — `5d984ef`                                                                      |
| C-DRY | Extract `LS_PROJECTS_OUTPUT` constant                  | ✅ Resolved | P3       | Fixed — `2489058`                                                                      |
| SEO-1 | `twitter:creator` into `siteConfig`                    | ✅ Resolved | P3       | Fixed — `2489058`                                                                      |
| T-W4  | Tests: `MotionProvider`, `TopologyLoader`, `not-found` | ✅ Resolved | P3       | Done — `f852da9` + `8a194c7`                                                           |
| T-W4b | `ProjectCard` fallback branch coverage                 | ✅ Resolved | P3       | Done — `f852da9`                                                                       |
| P3    | TopologyLoader loading skeleton                        | ✅ Resolved | P3       | Scan-line skeleton — `8a194c7`                                                         |
| P4    | LazyMotion bundle split                                | ✅ Resolved | P3       | Implemented — `m` + async `domAnimation`; 46KB gzip correctly deferred                 |

---

### 13.5 Overall Score Update

| Category          | Prev Score | Current Score | Delta     | Note                                                                                 |
| ----------------- | ---------- | ------------- | --------- | ------------------------------------------------------------------------------------ |
| Accessibility     | 9.5        | 9.5           | —         | A5 closed; no new gaps                                                               |
| Security          | 9.5        | 9.5           | —         | All P1 security items resolved                                                       |
| Performance       | 8.5        | 9.5           | ↑ 1.0     | Scan-line skeleton + LazyMotion bundle split — 46KB gzip deferred from critical path |
| Maintainability   | 9.0        | 9.5           | ↑ 0.5     | Dead code deleted, interface deduped, DRY applied                                    |
| Test Coverage     | 8.5        | 9.0           | ↑ 0.5     | 83 tests / 17 suites — ProjectCard, MotionProvider, TopologyLoader, not-found added  |
| Content Integrity | 8.0        | 8.0           | —         | C1/C2/C3 resolved; no remaining open items                                           |
| SEO / Metadata    | 8.5        | 9.0           | ↑ 0.5     | og:image confirmed correct; twitter handle in siteConfig                             |
| **Overall**       | **9.1**    | **9.5**       | **↑ 0.4** |                                                                                      |

_Overall score reflects completed P2/P3/P4 sweep. Full bundle analysis and deferred chunk inventory in Section 14. Remaining gap to 10/10 is WebGL test coverage (requires jest-canvas-mock) and any future content additions._

---

## Section 14 — Bundle Deep-Dive: Deferred vs Critical Path Analysis

_Audit date: 2026-04-27. Build: Next.js 16 + Turbopack, production build (`next build`). All sizes measured via `gzip -c | wc -c`._

---

### 14.1 Methodology

1. **Critical path identification** — parsed `.next/server/app/index.html` for all `<script src>` attributes pointing to `/_next/static/chunks/*.js`. These scripts are synchronously loaded before the page becomes interactive.
2. **Deferred chunk identification** — all other `.js` files in `.next/static/chunks/` not referenced by the HTML `<script>` tags are async-loaded by Next.js router or `next/dynamic` on demand.
3. **Content fingerprinting** — `grep -oc` pass over each chunk for known library keywords (`THREE`, `BufferGeometry`, `domAnimation`, `drag`, `inertia`, `router`, `motionValue`, etc.) to assign content labels.
4. **Size measurement** — raw byte count via `wc -c`, gzip byte count via `gzip -c file | wc -c`.

---

### 14.2 Critical Path Inventory

Chunks loaded synchronously on every page visit. Total: **198KB gzip**.

| Chunk                           | Raw    | Gzip       | Content                                                                         |
| ------------------------------- | ------ | ---------- | ------------------------------------------------------------------------------- |
| `edf728ade32a04ce.js`           | 219 KB | 68 KB      | Next.js App Router infrastructure (largest fixed cost)                          |
| `a6dad97d9634a72d.js`           | 109 KB | 38 KB      | React runtime + core polyfills                                                  |
| `38eb66a58ed5ffd7.js`           | 120 KB | 32 KB      | Next.js App Router (router, navigation, server context)                         |
| `2695c4dc2b67b76a.js`           | 46 KB  | 16 KB      | Framer Motion core + LazyMotion + 3 Lucide icons (Code2, Database, ShieldCheck) |
| `04b881840b2bc869.js`           | 34 KB  | 11 KB      | Framer Motion core (motionValue, AnimatePresence, useMotion\*)                  |
| `fb1b0b9da3f03023.js`           | 37 KB  | 8 KB       | Next.js runtime (webpack loader + chunk registry)                               |
| `d2be314c3ece3fbe.js`           | 29 KB  | 7 KB       | Next.js runtime (client-side routing internals)                                 |
| `3f39f81d822856a7.js`           | 13 KB  | 4 KB       | Next.js runtime (prefetch, hydration)                                           |
| `1f6884d3c8de7531.js`           | 11 KB  | 4 KB       | Next.js runtime (error overlay + HMR stubs)                                     |
| `turbopack-79cbdbe2f29209d1.js` | 9 KB   | 3 KB       | Turbopack module runtime                                                        |
| `f5a0d2972321b683.js`           | 3 KB   | 1 KB       | LazyMotion feature loader (app-level wrapper, `loadFeatures` fn)                |
| `ff1a16fafef87110.js`           | < 1 KB | < 1 KB     | Tiny stub (module boundary)                                                     |
| **TOTAL**                       |        | **198 KB** |                                                                                 |

CSS: `3b08974157d4078a.css` — 50 KB raw / **9 KB gzip** (Tailwind purged output, critical path).

**Framework overhead breakdown:**

- Next.js App Router (edf728 + 38eb66 + fb1b0b + d2be31 + 3f39f8 + 1f6884 + turbopack): ~137 KB gzip
- React runtime (a6dad9): ~38 KB gzip
- Framer Motion core (2695c4 + 04b881): ~27 KB gzip
- App code + LazyMotion wrapper: < 2 KB gzip

The application's own component code occupies under 1% of the critical path. Framework overhead is the dominant cost and cannot be reduced without changing the rendering strategy (e.g., static export).

---

### 14.3 Deferred Chunk Inventory

Chunks loaded asynchronously (non-blocking). Total: **374 KB gzip**.

| Chunk                 | Raw      | Gzip       | Trigger                              | Content                                                   |
| --------------------- | -------- | ---------- | ------------------------------------ | --------------------------------------------------------- |
| `60caaf671abf7197.js` | 1,058 KB | 318 KB     | `next/dynamic` → `TopologyLoader`    | Three.js + React Three Fiber + postprocessing             |
| `89ebb181bbbbfe1c.js` | 149 KB   | 46 KB      | `LazyMotion features={loadFeatures}` | Framer domAnimation (drag, pan, inertia, gesture engines) |
| `084417118677ff75.js` | 13 KB    | 5 KB       | Framer async split                   | Framer AnimationControls (`useAnimation`)                 |
| `7c92e96509cd355e.js` | 8 KB     | 3 KB       | Next.js async runtime                | Next.js dynamic import registry                           |
| **TOTAL**             |          | **374 KB** |                                      |                                                           |

**Total JS (all chunks): 572 KB gzip.**

---

### 14.4 Key Findings

#### F1 — Three.js Correctly Deferred ✅

`60caaf671abf7197.js` at **318 KB gzip** is the single largest chunk in the build — 55.6% of all deferred JS. It is loaded only after the main thread is free, triggered by `next/dynamic(() => import('./InteractiveTopology'))` inside `TopologyLoader.tsx`. This is the highest-impact deferral in the build and is already correctly handled. The `scanline` skeleton ensures a painted background is visible before the chunk loads.

Content confirmed: 167 occurrences of `BufferGeometry`/`ShaderMaterial`/`WebGL`/`PointsMaterial` keywords. Includes Three.js core, React Three Fiber renderer, and postprocessing pipeline (`UnrealBloomPass`, `EffectComposer`).

#### F2 — Framer domAnimation Correctly Deferred ✅

`89ebb181bbbbfe1c.js` at **46 KB gzip** contains the full Framer Motion gesture and animation engine (`drag`, `pan`, `inertia`, hover detection). Before the LazyMotion migration, this was bundled into the critical path. Post-migration, `MotionProvider` uses:

```ts
const loadFeatures = () => import('framer-motion').then((m) => m.domAnimation)
```

The `m` component variant (used in all animated components: `HeroHeader`, `SectionHeader`, `ProjectCard`, `FadeInSection`, `QAPhilosophyGrid`, `BioinformaticsGraphic`) strips down to a shell that consumes features only when `LazyMotion` resolves the async import. Result: **46 KB gzip removed from the blocking critical path**.

#### F3 — Framer Core Split Across Two Critical Chunks

Framer Motion core appears in both `2695c4dc2b67b76a.js` (16 KB) and `04b881840b2bc869.js` (11 KB), totalling **27 KB gzip** on the critical path. This is expected under Turbopack's chunk splitting strategy — the framework is split across multiple dependency boundary chunks. This is not a duplication issue; each chunk contains distinct Framer internals.

#### F4 — Lucide Icons Correctly Tree-Shaken ✅

Only 3 Lucide icons appear in the build (`Code2`, `Database`, `ShieldCheck`), all within `2695c4dc2b67b76a.js`. They are bundled alongside Framer Motion core — no separate Lucide chunk exists. Tree-shaking is working correctly; 3 named imports from the full 1,500+ icon library incur negligible bundle cost (~1–2 KB uncompressed).

#### F5 — Next.js App Router Is the Unavoidable Floor

The combined Next.js infrastructure (edf728 + 38eb66 + runtime chunks) accounts for ~137 KB gzip — the single largest category in the critical path. This is the framework's fixed cost for App Router with client-side navigation. It cannot be reduced without:

- Switching to static export (`output: 'export'`) — loses `ImageResponse`, server components
- Switching to Pages Router — not viable for a portfolio built on App Router conventions

**This is accepted as the baseline.**

---

### 14.5 Remaining Optimization Opportunities

| Opportunity                                | Potential Saving     | Complexity | Verdict                                                            |
| ------------------------------------------ | -------------------- | ---------- | ------------------------------------------------------------------ |
| Mobile-skip Three.js (static CSS fallback) | 318 KB gzip deferred | Medium     | UX decision required — would remove interactive topology on mobile |
| Image optimization (OG image)              | Negligible           | Low        | `ImageResponse` is already server-rendered on demand               |
| Additional Lucide icon trimming            | < 1 KB               | Trivial    | Only 3 icons present — no gain possible                            |
| Framer Motion core reduction               | Not possible         | —          | Core is required for `m` components and `MotionConfig`             |
| `next/dynamic` for `BioinformaticsGraphic` | < 5 KB gzip          | Low        | Not warranted — component is in the initial viewport               |

**The only meaningful remaining optimization is the mobile Three.js skip.** On mobile devices, the interactive topology canvas is purely decorative and requires device sensors + WebGL. Replacing it with a static CSS gradient on `(max-width: 768px)` would eliminate 318 KB gzip from mobile users' deferred download. This is a product/UX decision, not a pure engineering one.

---

### 14.6 Section 14 Summary

| Metric                       | Value                                         |
| ---------------------------- | --------------------------------------------- |
| Total JS (critical path)     | 198 KB gzip                                   |
| Total JS (deferred)          | 374 KB gzip                                   |
| Total JS (all chunks)        | 572 KB gzip                                   |
| CSS (critical path)          | 9 KB gzip                                     |
| Largest single chunk         | 318 KB gzip (Three.js — deferred)             |
| Largest critical chunk       | 68 KB gzip (Next.js App Router infra)         |
| LazyMotion saving            | 46 KB gzip moved to deferred                  |
| Remaining optimization space | 318 KB gzip (mobile Three.js skip — optional) |

Architecture verdict: **bundle shape is correct**. Heavy dependencies (Three.js, Framer gesture engine) are deferred. The critical path is dominated by unavoidable framework overhead, not application code. No structural changes are warranted at the current scale.
